#include<stdio.h>
#include<time.h>

int main()
{
    FILE* fp;
    int arr[10];
    fp=fopen("randomn.txt","r+");
    srand(time(NULL));
    int n1=100,n2=50;
    for(int i=0 ;i<10 ;i++ )
    fprintf(fp,"%d\n",rand()%(n1-n2)+n2);
    for(int i=0 ;i<10 ;i++ )
        f
    return 0;
}
